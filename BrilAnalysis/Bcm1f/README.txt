To run this software standalone on lxplus one must use python 2.6,
for there is no other standalone python installation.

Python 2.6 can be easily installed in any unix-like (e.g.linux,
macos x) system with the default location at /usr/bin.
